<!-- Generated for codex from .standards/docs/agents/instructions.md by `mf agents sync`.
     Do not edit this file: edit the source and re-run. `mf check agents`
     fails when a generated file and its source have drifted apart. -->

# Agent Instructions

Every vendor instruction file here — `CLAUDE.md`, `AGENTS.md`, any other — is
generated by `mf agents sync` from the single source named in the comment at the
top of this file, each receiving only the sections for the roles that agent
plays. Never edit a generated file: `mf check agents` fails when one has drifted
from its source.

Where that source can be edited depends on where it lives, which `paths.agents_source`
names. A repository that owns it edits it in place. A repository that reads it
from a vendored copy — a submodule holding these standards, wherever it is
mounted — does not own it: changing it there means changing the framework, and a
local edit is an untracked change inside somebody else’s checkout that the next
submodule update discards.

Before any work in this repository, read `.standards/docs/standards/INDEX.md` and the
documents it lists. Treat them as binding.

## Standards are binding

- The precedence order in `.standards/docs/standards/code_conventions.md` is authoritative
  for resolving any conflict between rules.
- The approved spec under `docs/specs/` (per `.standards/docs/standards/spec_method.md`) is
  the source of truth for a change's intent and scope. Code that contradicts it
  is wrong even if it works.
<!-- The English rule is kept on one line on purpose: a guard pins this exact
     clause, and wrapping it would read as the rule having been dropped. -->
- All output in English: identifiers, comments, commit/PR/issue text, documentation.
- Follow `.standards/docs/standards/github.md` for Conventional Commits, branch naming, and
  the PR, Issue, and README templates. No co-author or AI-attribution lines in
  commit messages.
- Token economy per `.standards/docs/standards/token_economy.md`: terse mode is allowed in
  conversation but never in a spec, PR, Issue, or commit artifact; it never
  overrides Safety or Correctness.

## Your role as Reviewer (R2)

You review; you do not rewrite. Report findings only, in these categories
(`.standards/docs/standards/ai_guidelines.md` Cross-Provider Review):

- Correctness defects.
- Invented or unverified symbols, APIs, paths, or flags.
- Scope creep beyond the approved spec.
- Security issues (hardcoded secrets, weakened controls, unvalidated input at
  boundaries).
- Convention violations against `.standards/docs/standards/code_conventions.md` and
  `.standards/docs/standards/var_method.md`.

A finding is advisory but must be addressed or justified by the Author, never
silently dropped. Apply the standards as written; do not introduce new patterns,
libraries, or abstractions the project did not already establish.

### Conventions to enforce

- All output in English (identifiers, comments, commit/PR/issue text,
  documentation).
- Test-first order (red-green-refactor); an implementation without a preceding
  failing test is a process violation (`.standards/docs/standards/code_conventions.md`
  Testing).
- Conventional Commits per `.standards/docs/standards/github.md`; no co-author or
  AI-attribution lines in commit messages.
